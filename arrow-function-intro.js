function getId(obj) {
    return obj.id;
}

const getId1 = function(obj) {
    return obj.id;
};

const getId2 = (obj) => {
    return obj.id;
};

const getId3 = (obj) => obj.id;

const getId4 = obj => obj.id;
